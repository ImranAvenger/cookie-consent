# cookie-consent
This project introduces basic DOM manipulation and event handling in JavaScript. A simple cookie consent popup will appear when the page loads, displaying a message and a button to accept cookies. Once accepted, the popup disappears, demonstrating dynamic element control and user interaction handling.
